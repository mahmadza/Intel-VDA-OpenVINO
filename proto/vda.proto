syntax = "proto3";
package vda;

service VideoService {
  rpc Ping (Empty) returns (Pong);
  rpc ProcessVideo (VideoRequest) returns (stream ProgressUpdate); 
}

message VideoRequest { string file_path = 1; }
message Empty {}
message Pong { string message = 1; }
message ProgressUpdate {
  string status = 1;
  float percentage = 2;
}